import * as TasksListItemTheme from './TaskListItem/Theme'

export interface ThemeProps {
  row: TasksListItemTheme.ThemeProps
}

export const DEFAULT_THEME: ThemeProps = {
  row: TasksListItemTheme.DEFAULT_THEME
}
