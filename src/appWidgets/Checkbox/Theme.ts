export interface CheckboxThemeProps {
  bgColor: string;
  textColor: string;
  borderColor: string;
}

export const DEFAULT_THEME = {
  bgColor: '#FFFFFF',
  textColor: '#6576AE',
  borderColor: '#CED4E8',
}
