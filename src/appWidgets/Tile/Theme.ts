export interface ThemeProps {
  unActiveTextColor: string;
  activeTextColor: string;
  unActiveBgColor: string;
  shadow: string;
}

export const defaultTheme = {
  unActiveTextColor: '#2e9ec3',
  activeTextColor: 'white',
  unActiveBgColor: '#dae6ea',
  shadow: '#f4851a'
}
