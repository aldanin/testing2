export interface ThemeProps {
  textColor: string,
  subTextColor: string;
  tooltipHeaderBg: string;
  iconColor: string;
}

export const defaultTheme = {
  textColor: 'black',
  subTextColor: '#9fa1a2',
  tooltipHeaderBg: '#2e9ec3',
  iconColor: '#9fa1a2',
};
