export interface ThemeProps {
  textColor: string,
  borderTopColor: string;
  bgColor: string;
  boxShadow: string;
}

export const defaultTheme = {
  textColor: 'black',
  borderTopColor: '#51aecb',
  bgColor: '#fefefe',
  boxShadow: 'rgba(23, 23, 23, 0.13)',
}
